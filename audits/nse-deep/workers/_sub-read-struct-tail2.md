# sub-read-struct-tail2 — structural tail (8 files), READ-ONLY, NSE @ f9e8bc5

## 1. Euler/TransversePacketForwardGradeBounds.lean — 5 decls: 3 nonneg lemmas, `structure GradeGuards (C : ℝ) : Prop` (:48-53), `grade_fields` (:65).
- **CONSTRUCTED, unconditionally.** `EulerTransversePacketForward.Budget.GradeGuards` is built by anonymous constructor at `Euler/PacketForwardCommonRadius.lean:43` (`gradeRadius_guards`, from `gradeRadius_bounds` + `hR`), for ANY `0 ≤ C`; also `exists_grade_radius` (:45-49) and `commonRadius_guards` (:94-107) / `exists_common_radius` (:109) supply it with `C = 1` and `C = terminalCost`. So not an unsupplied hypothesis. Note the same-named twins in `EulerTransversePacketJoin.Budget` (`Euler/PacketJoinedGradeBounds.lean:84`), `EulerMeanPacketProvider.Budget`, `EulerTransversePacketPrimary.Budget` — distinct arities (ours takes `C`).
- Vacuity: `C = 0` satisfies all four guard inequalities trivially (`L.R ≥ 1` by `L.radius_one`), but `grade_fields`'s conclusion (`WordBound 6 L.R 1 e` for 5 fields) does not mention `C`; `C` only scales the hypotheses `hforce`/`hinitial`. So a degenerate `C = 0` makes the HYPOTHESES stronger, not the conclusion weaker — safe direction. Live callers use `C = 1` (`PacketForwardStepBudget.lean:22`) and `C = wordCost (Fin 4) 6 δ*‖ξ‖` (`PacketForwardInitializedProfiles.lean:36`), i.e. non-degenerate witnesses exist. **OK**
- Junk values: no `/`, no `⁻¹` in this file. `c` is guarded `0 < c` in-signature (:57). Kernel: no inductive/rec/decide/termination_by; one `cases i using Fin.cases` on `Fin 4` inside a proof (:79) — benign; largest numeral 6.
- Verdict: **OK (clean)**.

## 2. Euler/ParentPacketSourceData.lean — 8 decls: `structure LowBounds (G : Parent)` (:19-36, Type-valued, 5 real fields + 8 guards), `frame_within`/`first_within` (:42,:48), `meanData` (:54), `transverseData` (:95), `sourceAgreement` (:112), `history_small` (:123), `historyData` (:129).
- **CONSTRUCTED, and BOTH a degenerate and a non-degenerate witness exist.**
  - degenerate live base: `Euler/BaseEulerState.lean:53` `initialLowBounds`, with `initialLowBounds_values` (:56-61) proving BY `rfl`-chain `Be = initialCoefficientCost, Bc = 0, L = 0, r = 0, K = initialCoefficientCost`; restricted at `Euler/BasePacketSetup.lean:67`, and `packetBase_boundary_zero` (:73-74) re-states `L = 0` by `rfl`. With `r = 0` the field `core_lower` (:33-34) is VACUOUS (no `x` with `‖G.ell • x‖ < 0`) and every `Bc*r^3` boundary term in `small` (:36) is identically 0 — this is the same degeneracy the audit already logged for `MeanPacketData`, here with its exact provenance.
  - **non-degenerate alternative: `Euler/ParentFirstPacketLowGuards.lean:77`** builds `lowBoundsFromPhysical CM Cnew (boundaryLocalizationC1*Cnew+1) A.ell Knew`, i.e. `r = A.ell > 0` (`A.ell_pos`, discharged at :80) and `L = C1*Cnew+1 ≥ 1`. Generic constructors: `lowBoundsOfPhysical` (`Euler/ParentPacketPhysicalCoefficients.lean:89`), `lowBoundsFromPhysical` (`Euler/ParentEulerLowBounds.lean:62`), `updateLowBounds` (`:78`, which PRESERVES `H.r` at :86 — so a chain started at the base keeps `r = 0` unless it re-enters through :77).
- Sharpness note (new): the only artifact-wide fact about `initialCoefficientCost` is `0 ≤` (`Euler/BaseEulerGuards.lean:19,26`); nothing proves `0 < initialCoefficientCost`. Direction is SAFE: `Be`/`K` are the *ceilings* (`-Be*‖v‖^2 ≤ ⟪M0 x v,v⟫`, `⟪H x v,v⟫ ≤ K*‖v‖^2`), so smaller = stronger hypothesis, and these are proved, not assumed, at the base.
- Junk values: only literal denominators (`1/4` :29, `T^2/2` :36, `1/2` :36); no variable division or `⁻¹` in any statement of this file. Kernel: no inductive/rec/decide/termination_by/deriving; largest numeral 4 (as `1/4`) and exponent 3.
- Verdict: **OK, with one NOTE** (degenerate base witness `Bc = L = r = 0`, non-degenerate alternative present at `ParentFirstPacketLowGuards.lean:77`).

## 3. Euler/PacketProfileParity.lean — 8 decls: `structure ProfileParity (T) (a : Profile) : Prop` (:12-18), `.zero` (:22), `.prefixOdd` (:34), `.changeTime` (:40), 3 derivative-parity lemmas (:50,:53,:56), `JointOdd.changeTime` (:61).
- **CONSTRUCTED with a real base AND a non-degenerate witness.** Base: `.zero` (:22) for `a = 0`. Non-degenerate: `ProfileParity.primary` (`Euler/PacketPrimaryParity.lean:17-25`) for `primaryProfile O (G.vector I) (G.scalar I)` from an arbitrary `Forcing`, and the recursion closes via `ProfileParity.step` (`Euler/PacketProfileStepParity.lean:31-32`) / `.joinedStep` (`Euler/PacketJoinedStepParity.lean:33`), consumed by `profiles_parity` (`Euler/PacketProfilesParity.lean:30-45`, base `.zero` at :36). So NOT closure-without-a-base and NOT zero-only.
- The three `*Derivative_odd` lemmas (:50-57) require a `ProfileRegularity` (:46) as well; the parity hypothesis `H` is `include`d (:48) and genuinely used.
- Junk values: none — no `/`, no `⁻¹` anywhere in the file. Kernel: no inductive/rec/decide/termination_by; no numeral above 0. `Fact (0 < P)` at :45 is not shadowed by any theorem in this file.
- Verdict: **OK (clean)** — one line: declares `ProfileParity`, base at :22, non-degenerate witness at `PacketPrimaryParity.lean:17`, clean.

## 4. Euler/PacketPrimaryGradeBounds.lean — 5 decls: `structure GradeGuards : Prop` (`EulerTransversePacketPrimary.Budget`, :29-34), `grade_fields` (:42), `profile_budget` (:89), `scaled_initialData_bound` (:140), `primary_profile_budget` (:151).
- **CONSTRUCTED.** `EulerTransversePacketPrimary.Budget.GradeGuards` is supplied at `Euler/PacketInitializedCanonicalRadius.lean:84-89` (`initializedRadius_guards`, from `(primaryRadiusPrimary L δ).gradeRadius_guards` with `C = wordCost (Fin 4) 6 δ*‖ξ‖`, nonnegativity discharged at :85). Distinct from the `EulerTransversePacketForward` twin of file 1 (same field names, different `Budget`).
- **NOTE (vacuous-by-construction components, benign):** `profile_budget` (:89) discharges 2 of the 7 `ProfileBudget` fields — `mean` and `meanDerivative` (`Euler/PacketProfileBudget.lean:19-20`) — with `hz := Field.wordBound_normalized_of_zero (Field.zero P D.T) (fun _ _ _ => rfl)` (:97-98, used at :106-107). I.e. the primary's mean field is IDENTICALLY ZERO by `rfl`, so those two components carry no information. This is structurally correct (the mean part enters only at the mean solve) but it means "primary profile budget" is a 5-of-7 statement.
- Degeneracy of the live `C`: on the terminal-datum path `C = wordCost (Fin 4) 6 δ*‖ξ‖` with `ξ : U` NOT constrained nonzero (:133). `ξ = 0` gives `C = 0` and the whole datum `α • ξ = 0`; both `grade_fields` and `primary_profile_budget` then say something about the zero datum. Safe (the statements are ∀-quantified in `ξ`, so `ξ ≠ 0` instances are included) — recorded only because it is the same pattern as the flagged degenerate witnesses.
- Junk values: no `/` and no `⁻¹` in any statement; `α` is guarded `0 < α` in-signature (:36,:134), `δ` guarded `0 < δ` (:133), `τ` guarded `0 < τ < D.T` (:22). This file is a clean POSITIVE CONTROL: every guard is in-signature.
- Kernel: no inductive/rec/decide/termination_by/deriving; one `cases i using Fin.cases` on `Fin 4` in a proof (:145) — benign. Largest numeral 6.
- Verdict: **OK + 1 NOTE**.

## 5. Euler/PacketForwardChildLowBounds.lean — 6 decls: `forwardPressureTerm_eq_coefficient` (:20), Prop-def `ForwardSourceErrors` (:59-69), `forwardSourceErrors_of_global` (:73), and 3 low-bound theorems (:91 good, :126 early, :163 whole-horizon).
- **The Prop-def IS constructed.** `ForwardSourceErrors` appears in hypothesis position at :93,:128,:165, `ParentPacketInitialLowBounds.lean:94,:109`, `ParentPacketChildLowGuards.lean:77` and as a structure FIELD `errors` at `Euler/ParentGeometryForwardChoice.lean:90` / `ParentGeometryForwardChoiceNoOptions.lean:114`. It is genuinely SUPPLIED twice: (a) `forwardSourceErrors_of_global` (:73-88) from strict (`<`) global packet errors, and (b) inline in `exists_geometryForwardChoice` (`Euler/ParentGeometryForwardChoice.lean:115-120`, which re-uses `forwardPressureTerm_eq_coefficient` from :20 of my file) with the NON-ZERO error levels `ev = ep = k^(-(1/4 : ℝ))` (:93). So not an unsupplied hypothesis and not a zero-error degeneracy.
- **JUNK-VALUE SWEEP — this file is a clean POSITIVE CONTROL.** Two potential division sites, both guarded in the same signature: (i) `scaledTime 0 F.a F.epsilon t` (:94,:129,:180) unfolds to `(F.a/F.epsilon)*(t-0)` (`Euler/PacketPropagationTime.lean:12`) with `F.epsilon := Real.sqrt (F.a/F.shear)` (`Euler/PacketSourceGeometryData.lean` epsilon def) — nonzero because `G : ForwardGuards F` is IN THIS FILE'S signature (:53) and carries `coupling_lower : 1/2 ≤ P.a` + `shear_pos : 0 < P.shear`, yielding `ForwardGuards.epsilon_pos` (`Euler/PacketForwardGeometryData.lean:51-55`); (ii) `A.ell⁻¹ • x` (:104,:139) with `A.ell_pos` a field of `Parent`. Also `hk : k*κ=1` (:50) constrains both `k ≠ 0` and `κ ≠ 0` in-signature. Additionally the `scaledTime` threshold is only a CASE SPLIT (:180 `by_cases`), so even a junk value could not make the whole-horizon conclusion trivial.
- Kernel: no inductive/rec/decide/termination_by/deriving/metaprogramming. Largest numeral in the file: 2 (and exponent 2); `nlinarith only [...]` used with explicit term lists (:184-194) — no numeric blow-up.
- Verdict: **OK (clean)**.

## 6. Euler/PacketCylinderPrefixLocality.lean — 5 decls: `structure PrefixLocality (T) (p) (a) (S) : Prop` (:12-18), `PrefixFields.knownJet_angleIndependent` (:22), `AngleIndependentJet.slowAdvection` (:43), `AngleIndependentJet.fastAdvection_zero` (:51), `PrefixFields.nonlinear_angleIndependent` (:57).
- **CONSTRUCTED:** `Euler/PacketProfileRegularity.lean:83` builds `PrefixLocality T p a S` from per-index `ProfileRegularity` (the same record used live at `PacketPrimaryGradeBounds.lean:91,:153`). Consumers: `PacketCylinderHighSupport.lean:39,:65`, `PacketCylinderRecursiveAdmissibility.lean:46` (there `S := D.support`).
- **NON-VACUITY WITNESS for the `x ∉ S` hypothesis (:24,:59):** on the live path `S = D.support` which is COMPACT (`Data.support_compact`, supplied at `ParentPacketSourceData.lean:99`), and `Space = EuclideanSpace ℝ (Fin 3)` (`NavierStokes/MovingFrameODE.lean:25`) is non-compact, so `Sᶜ` is nonempty and both exterior theorems have content. Not `S = univ`-vacuous.
- NOTE (definitional, benign): the `i = 0` branches (:28-30 and the `i ≠ p, i ≥ p` branch :41) are discharged by `AngleIndependentJet.zero` because `velocityJet s a z 0 = 0` BY DEFINITION (`Euler/PacketProfileRecursion.lean:44-46`, `if i=0 then 0 else ...`). So grade 0 carries no information anywhere in this family; consistent with the explicit `ha : a 0 = 0` hypotheses elsewhere (`PacketFiniteParity.lean:116`).
- `fastAdvection_zero` (:51) is an EXACT identity `= 0`, but it is not a junk-value collapse: it holds because `hK.angular` forces the angular slot to vanish, and it is used as a rewrite (:73), not as a headline bound.
- Junk values: none (no `/`, no `⁻¹` anywhere in the file). Kernel: no inductive/rec/decide/termination_by; only term-level `if` on `Nat` equality inside the cited definitions; `split_ifs`/`sum_congr` over `Finset.range (p+1)`; largest numeral 1.
- Verdict: **OK (clean)**.

## 7. Euler/PacketCylinderHighParity.lean — 5 decls: `structure CoefficientEven (T) (O) : Prop` (:13-16) + `meanForce_odd` (:20), `highForce_odd` (:31), `actualMean_odd` (:47), `actualHighForce_odd` (:63).
- **CONSTRUCTED twice, for the ACTUAL operators:** `sourceCoefficientEven` (`Euler/PacketSourceParity.lean:19-22`, `CoefficientEven M.T (sourceOperators P M D I)`) and `joinedSourceCoefficientEven` (`Euler/PacketJoinedSourceProfiles.lean:45-48`). Both are consumed on the live parity path (`PacketProfilesParity.lean:21`, `PacketForwardInitializedFieldParity.lean:39`, `PacketInitializedFieldParity.lean:41`). `PrefixOdd` (the other hypothesis) is supplied by `ProfileParity.prefixOdd` (`PacketProfileParity.lean:35`) — see file 3.
- Index hygiene checked: `highForce_odd` needs `hp : 2 ≤ p` (:32) precisely because it uses `H.high 1` / `F.high 1` (:42-43); `meanForce_odd` needs only `1 ≤ p` (:21). No stronger-than-needed twin: the `2 ≤ p` version is the one that actually needs `1 < p`, and `actualHighForce_odd` (:66) forwards `2 ≤ p` and weakens to `1 ≤ p` via `by omega` (:74) exactly where the weaker form is used.
- Junk values: none (no `/`, no `⁻¹`). Kernel: no inductive/rec/decide/termination_by; `p-1` is truncated ℕ subtraction always guarded by `1 ≤ p`/`2 ≤ p` + `by omega`; largest numeral 2.
- Verdict: **OK (clean)**.

## 8. Euler/PacketCylinderForcingParity.lean — 5 decls: `structure PrefixOdd (T) (p) (a) : Prop` (:12-15), `JointOdd.convolution` (:17), `PrefixOdd.knownJet_value_odd` (:34), `PrefixFields.nonlinear_odd` (:51), `PrefixFields.knownForce_odd` (:62).
- **CONSTRUCTED:** `ProfileParity.prefixOdd` (`Euler/PacketProfileParity.lean:34-38`) and `PacketFiniteParity.lean:123` (`let Hpre : PrefixOdd T (N+1) a`). Behind it sit `ProfileParity.zero` (base) and the non-degenerate `ProfileParity.primary` (`PacketPrimaryParity.lean:17`), so this is a supplied predicate with a live non-zero witness — NOT closure-without-a-base.
- NOTE (same definitional convention as file 6): the `i = 0` case at :38-40 is closed by `simp` because `velocityJet _ a z 0 = 0` (`PacketProfileRecursion.lean:44-45`), and the `i > p` case at :49 by `Prod.fst_zero`. So 2 of the 4 index branches of `knownJet_value_odd` are about the zero jet.
- `hI`/`hM`/`hN` (:53-54,:67-69) are exactly the three fields of `CoefficientEven` (file 7) passed unbundled — the two forms are equivalent, and both are supplied (`PacketSourceParity.lean:22`), so no unsupplied-twin problem.
- Junk values: none. Kernel: no inductive/rec/decide/termination_by/deriving/`native_decide`/`Acc.rec`; sums over `Finset.range (M+1)`; largest numeral 1.
- Verdict: **OK (clean)**.

## Block summary
- 8 files, 47 decls read. Structures/Prop-defs declared: `GradeGuards` (Forward, :48), `LowBounds` (:19), `ProfileParity` (:12), `GradeGuards` (Primary, :29), `ForwardSourceErrors` (Prop-def, :59), `PrefixLocality` (:12), `CoefficientEven` (:13), `PrefixOdd` (:12) — **ALL EIGHT ARE CONSTRUCTED**, each with file:line above. No unsupplied hypothesis, no closure-without-a-base in this block.
- **JUNK-VALUE SWEEP: ZERO exposures.** Across all 8 files the only `/` are literal denominators (`1/2`, `1/4`, `T^2/2`) and the only `⁻¹`/variable division sites are `A.ell⁻¹` (proof-local, `Parent.ell_pos`) and `scaledTime`'s `F.a/F.epsilon`, which is guarded IN-SIGNATURE by `G : ForwardGuards F` -> `epsilon_pos` (`PacketForwardGeometryData.lean:51-55`). Files 4 and 5 are useful POSITIVE CONTROLS.
- **KERNEL RISK: ZERO new flags.** No `inductive`, no `.rec`/`Nat.rec`/`Acc.rec`, no `termination_by`, no `deriving`, no `decide`/`native_decide`, no metaprogramming, no Type-valued `if`. Largest numeral anywhere in the 8 files: **6** (the fixed Sobolev/word order) — plus `Fin 4` (< 22). Two proof-local `cases i using Fin.cases` on `Fin 4` (`TransversePacketForwardGradeBounds.lean:79`, `PacketPrimaryGradeBounds.lean:145`), benign.
- No variable-shadowing defeat of a `Fact (0 < P)` instance: the `Fact` sections (files 1,3,4,6,7,8) are never re-bound by a theorem argument named `P`.
- Most important finding: the `LowBounds` degenerate-base chain in file 2 (`BaseEulerState.lean:56-61`: `Bc = L = r = 0` by `rfl`; `updateLowBounds` PRESERVES `r` at `ParentEulerLowBounds.lean:86`), with the non-degenerate escape confirmed at `ParentFirstPacketLowGuards.lean:77` (`r = A.ell > 0`, `L = C1*Cnew+1`).

