# NSE no-supplier audit (read-only, commit f9e8bc5)

No build was run. I searched only `*.lean` source with grep and inspected candidate contexts.

## 1. `NavierStokes.PhysicalStageBounds.MeanData`

**Verdict: UNSUPPLIED.** Declaration is `PhysicalStageBounds.lean:154-172`; it is a Prop structure with fields `firstBand` through `native_class`. The candidate supplied 49 hypothesis sites in 4 files; the sites are in `PhysicalStageBounds.lean` (e.g. theorem binders at `174-206`, API/field binders at `287-350`, family binders at `400-537`), `InitializedPhysicalBackground.lean:78-284`, `PhysicalStageSupport.lean:117-365`, and `ActualIterationLedger.lean:329-348`. I examined the complete occurrences in those four files; every candidate occurrence is a declaration/theorem/structure binder or a field access on such a binder, not a construction.

I found no conclusion of `MeanData` without a `MeanData` hypothesis, no `MeanData.mk`, no anonymous expected-`MeanData` constructor, no instance, and no constructed parent carrying a `MeanData` field. The similarly shaped `NavierStokes.ActualPhysicalStageBounds.MeanInput` is a different name/namespace: it is declared at `ActualPhysicalStageBounds.lean:30-43` and constructed by `MeanInput.ofMoving` at `ActualPhysicalStageBounds.lean:47-67`; it cannot supply `PhysicalStageBounds.MeanData` (route 5 checked). The theorem families look like an intended physical initialization/support branch (`InitializedPhysicalBackground.lean:75-103`, `PhysicalStageSupport.lean:208-222`) but the actual replacement interface is `MeanInput` (`ActualPhysicalStageBounds.lean:27-52`), so these hypotheses look like a dead/unreachable branch rather than the live actual chain.

## 2. `NavierStokes.ParticularWaveAssembly.BandCharts`

**Verdict: SUPPLIED — route 2 at `ActualReferenceRebase.lean:246-250`.** `BandCharts` is the three-field structure declared at `ParticularWaveAssembly.lean:520-523`; `rebaseAssembly` assigns `charts := ⟨fun _ => id, fun _ => 0, fun _ => 1⟩` at `ActualReferenceRebase.lean:246-250`, an anonymous constructor against the expected `BandCharts P`. Route 4 also applies: `AssemblyData` has field `charts : BandCharts P` at `ParticularWaveAssembly.lean:1736-1739`, and the actual assembly constructs that parent with a chart record at `ActualParticularStageControls.lean:162-170`. The 30 candidate hypothesis sites are in exactly 2 files: `ParticularWaveAssembly.lean:537-1479` and `PeriodizedWaveBounds.lean:1589-1669`; they are parameters to definitions/theorems. This is load-bearing in the actual particular-wave branch: `AssemblyData.wave` consumes `D.charts` at `ParticularWaveAssembly.lean:1751-1755`, and the periodized bounds API consumes `charts` at `PeriodizedWaveBounds.lean:1589-1669`.

## 3. `NavierStokes.SignedMeanGain.MovingField`

**Verdict: SUPPLIED — route 4 at `MeanStateRegularity.lean:454-470` (fields `source` and `updated_source`).** The predicate is the alias defined at `SignedMeanGain.lean:654-655`. `SignedMeanGain.LocalData` contains fields of exactly this type at `SignedMeanGain.lean:820-835`; `MeanStateRegularity.localData` constructs that parent at `MeanStateRegularity.lean:444-470`, filling `source` at `465` and `updated_source` at `466-467`. (The same parent is returned by the regularity adapter at `MeanStateRegularity.lean:475-487`.) The 26 candidate hypothesis sites are in 6 files: `SignedMeanGain.lean`, `ActualSignedMeanBinding.lean:678`, `CorrectionStep.lean:7737-9702`, `CrossBasedMeanComposition.lean:61-653`, `CorrectionAnalyticStep.lean:137-610`, and `BandReindexedSignedMeanGain.lean:154-155`; the candidate occurrences are binders/derived uses, while the parent construction above supplies the field. These hypotheses are on the signed mean/correction estimate chain: `signed_mean_gain_of_cross` takes `hX`/`hS` at `SignedMeanGain.lean:970-988`, and `native_signed_mean_gain` takes them at `SignedMeanGain.lean:1326-1352`.

## 4. `NavierStokes.MovingMomentBounds.Support`

**Verdict: SUPPLIED — route 4 at `MovingMomentBounds.lean:337-350`; independently also at `MeanLocalDefectBounds.lean:272-285`.** `Support` is the predicate definition at `MovingMomentBounds.lean:118-119`. Its parent `SupportedTriple` has `radial`, `angular`, and `axial` fields of `Support` at `MovingMomentBounds.lean:186-190`. `rankStage_defect_class` constructs a `SupportedTriple` at `MovingMomentBounds.lean:337-350`, supplying all three fields (the exact analogous construction is `MeanLocalDefectBounds.lean:272-285`). The 19 candidate hypothesis sites are in 2 files, `MovingMomentBounds.lean` and `MeanLocalDefectBounds.lean`; their uses are binders/derived support terms. This is load-bearing in the moving-moment remainder path: `remainders_mem` takes `hms`, `hhs`, and `hWs` at `MovingMomentBounds.lean:228-240`, and the rank-stage theorem takes support hypotheses at `MovingMomentBounds.lean:298-315`.

## 5. `NavierStokes.PeriodicIntegration.UnitPeriods`

**Verdict: UNSUPPLIED.** `UnitPeriods` is the forall predicate at `PeriodicIntegration.lean:40-41`. I examined all 17 candidate hypothesis sites in 3 files: `PeriodicIntegration.lean:123-185`, `PeriodicSobolev.lean:266-315`, and `PeriodicUniqueness.lean:324-623`. Every candidate site is a hypothesis binder (or a derived local conclusion that still uses existing `UnitPeriods` hypotheses); examples are `cubeIntegral_partial_eq_zero` at `PeriodicIntegration.lean:123-124`, `component_periodic`/`spatial_partial_periodic` at `PeriodicUniqueness.lean:324-332`, and `unitPeriods_sub` at `PeriodicUniqueness.lean:463-467`. The local proofs at `PeriodicIntegration.lean:185-189` and `PeriodicUniqueness.lean:396-400` assume `hpf`/`hpg`, so they do not supply a base instance. I found no no-hypothesis conclusion, instance, parent field, or anonymous constructor. No same-named twin was found. The theorems are load-bearing *conditional* periodic integration/energy lemmas (`PeriodicIntegration.lean:122-124`, `PeriodicUniqueness.lean:414-447`), but the artifact has no supplier for their periodicity premise.

## 6. `NavierStokes.PhysicalClassBounds.SourceBounds`

**Verdict: SUPPLIED — route 1 at `PhysicalClassBounds.lean:251-262` and `264-273`.** `SourceBounds` is declared at `PhysicalClassBounds.lean:229-238`. `SourceBounds.of_wave` concludes `SourceBounds ...` at `PhysicalClassBounds.lean:260` with no `SourceBounds` hypothesis, and its body constructs it with `⟨...⟩` at `261-262`; `SourceBounds.of_mean` does the same at `PhysicalClassBounds.lean:271-273`. The 14 candidate hypothesis sites are in 2 files: `PhysicalClassBounds.lean` (e.g. `chart_bound` at `240-249`, `map` at `275-280`, and downstream bridge binders at `340-569`) and `PhysicalCopyBounds.lean:356-468,681-690`. The downstream uses are load-bearing in the physical copy/jet chain: `CommonChart.amplitude_bound` calls `hs.chart_bound` at `PhysicalCopyBounds.lean:356-375`, and `localStrippedClass` consumes it at `PhysicalCopyBounds.lean:437-456`. The different namespace `NavierStokes.LocalPhysicalCopyBounds.LocalSourceBounds` is a distinct twin and does not count for this predicate; it is declared at `LocalPhysicalCopyBounds.lean:522`.

## Calibration: `NavierStokes.LocalResidualGrouping.ExtractionRegular`

**Verdict: SUPPLIED — route 2 (and route 1) at `ActualInitialization.lean:848-853`.** The structure is declared at `LocalResidualGrouping.lean:210-223`. `initial_extraction_regular` has a conclusion of this predicate with no such hypothesis at `ActualInitialization.lean:848-851`; its proof opens the expected structure goal with `constructor` at `ActualInitialization.lean:852` and fills all fields through `ActualInitialization.lean:853` onward. Thus the known supplier is the fully qualified `NavierStokes.LocalResidualGrouping.ExtractionRegular`; the same-named `NavierStokes.HarmonicResidual.ExtractionRegular` declared at `HarmonicResidual.lean:1461` is a different predicate and does not count.
